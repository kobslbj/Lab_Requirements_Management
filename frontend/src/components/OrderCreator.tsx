'use client';

import { Button } from '@nextui-org/button';

export default function OrderCreator() {
  return (
    <Button radius="sm" size="lg" variant="faded" className="text-sm">
      新增委託單
    </Button>
  );
}
